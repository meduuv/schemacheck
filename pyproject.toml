[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "schemacheck"
version = "0.1.0"
description = "Lightweight validation helpers for JSON-like data"
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
[tool.pytest.ini_options]
pythonpath = ["src"]
